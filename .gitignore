# Only the published dashboard, feeds, and repo housekeeping files belong here.
# Ignore everything, then explicitly allow the files we publish.
*

!index.html
!feed.json
!rss.xml
!publish.bat
!README.md
!.gitignore
!.nojekyll

# Never commit the run log.
publish-log.txt
